const Layout = ({ children }: { children: React.ReactNode }) => {
  return (
    <>
      <>{children}</>
      <h2 className='bg-blue-300'>Here will be Contact section with form</h2>
    </>
  );
};

export default Layout;
